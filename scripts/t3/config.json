{
  "status": "PROPOSED",
  "task": "S2-DS-07",
  "title": "T3 candidate-ranking head - no learned reranker beats retrieval",
  "scope": {
    "cohort": "C1",
    "user_slice": "user_id % 4 == 1",
    "measured_users": 97279,
    "data": "frozen v1 artifacts, not rebuilt and not extended",
    "test_seal": {
      "test_rows_used": 0,
      "measured": "_SEAL/test_seal_measured.json"
    }
  },
  "encoder": {
    "channels": [
      "category_id",
      "event_type_id"
    ],
    "use_gap": true,
    "hidden": 128,
    "layers": 1,
    "dropout": 0.3,
    "core": "gru",
    "note": "read from S2-DS-01's selected_config(); never restated by hand"
  },
  "retrieval": {
    "selected": "cooccurrence_then_popularity",
    "candidates": "output/t3_candidate_lists_train_anchors_v1.parquet",
    "anchors": 107484,
    "rebuilt_because": "the frozen artifact built lists only for query items with a VALIDATION positive; rebuild_candidates.py extends it from TRAIN anchors and asserts the frozen lists come back byte-identical"
  },
  "selected": {
    "deliverable": "the frozen retrieval order, not a trained model",
    "reason": "no loss beat it on any seed. The query-aware cross-feature reranker peaks at 0.259 against 0.2707",
    "acceptance_bar": {
      "min_macro_gain": 0.01,
      "interval_above_zero": true,
      "fixed_before_the_run": true,
      "met": false
    }
  },
  "results": {
    "baseline_macro": 0.2707,
    "baseline_micro": 0.2046,
    "ceiling_macro": 0.8795,
    "perfect_reranking_of_the_same_lists": 0.8782,
    "headroom_a_reranker_could_reach": 0.6075
  },
  "do_not_load": [
    "output/t3_listwise.pt",
    "output/t3_pointwise.pt"
  ],
  "consumed_by": []
}